# Loopy 진행 상황

작업 하나 = 커밋 하나 = 이 표의 한 줄. **작업이 끝날 때마다 이 파일도 같이 고친다.**
저장소 밖 문서는 반드시 뒤처지므로, "지금 어디까지 됐나"의 답은 항상 여기다.

번호는 **평평하게 `작업 N`** 만 쓴다. (9-1/9-1b 같은 하위번호는 과거 흔적일 뿐, 앞으로 쓰지 않는다.)

---

## 끝난 작업

| # | 한 일 | 남긴 것 |
|---|---|---|
| 5 | MaterialType/MaterialRegistry 를 BlockDef 로 흡수 | 타입 정의가 BlockDef 하나. 도메인은 TypeKinds(kind)만 앎 |
| 6 | 데이터 통합 | 타임라인이 캔버스 덩어리 구조를 보존. 죽은 macro//Migration 삭제 |
| 7 | 에디터 상태 홀더 | 순수 EditorUi/EditorEvent/reduce. 화면은 그리기만 |
| 8 | God object 분해 | OverlayService 763→616, MainActivity 698→324 |
| 9 | BlockSpec 이중화 종료 + 위생 | 모양·색·문장이 BlockDef 한 곳. 미사용 import 113개 정리 |
| 10 | 빌드 불러오기 **기반** | ExecContext=능력가방(data class), MaterialSource port, BuildPick |
| 11 | 빌드 불러오기 **UI** | 저장된 빌드를 이름으로 골라 꽂음 |
| 12 | 슬롯편집 **Step1**(뼈대) | Material.slots + 직렬화 + Evaluator + if 홈 조건. 겉보기 무변화 |
| 13 | 에디터 기하/렌더 정리 | C 세로 맞물림 공식, 오른쪽아래 모서리, 무한 모눈, 유색 뉴모피즘 |
| 14 | C 좌우 맞물림 + 번짐 | 입 벽을 INDENT 에서 파생(8dp 어긋남 해소), 어두운 그림자 제거 |
| 15 | 슬롯편집 **Step2** | 홈에 블록 꽂기(눌러서 고르기), 중첩 렌더, 비교 블록, 트리연산 슬롯 인지 |
| 16 | 조립 규칙 교정 | 값 블록은 홈에만(스택 금지), 마개 아래 연결 차단, 맨 위 삽입 시 덩어리가 위로 자람 |
| 17 | 연결점 규칙 일원화 | + 버튼도 레이아웃 연결점을 사용(우회 제거), 맨 위 판정을 잡은 블록 높이만큼 보정, 빈 홈은 파인 모양 |
| 18 | 결합 규칙을 한 곳으로 | `canPlaceAt` 신설 — 모자도 다른 블록과 같은 경로로 결합(줄기 맨 위). 시트에서도 홈을 채울 수 있게 |

## 지금 하는 일

**슬롯 편집**(값·조건 블록을 홈에 꽂기) — 3단계 중 1단계 완료.

- [x] **Step 1** 데이터·평가 뼈대 (작업 12)
- [x] **Step 2** 홈에 꽂기 — 빈 홈을 눌러 고름, 중첩 렌더, 조건 블록 1개(비교) (작업 15)
- [ ] **Step 3** 값·조건 블록 더: 그리고/또는/아님, 변수 리포터(둥근 홈), 나중에 이미지 인식

## 다음에 볼 것

- 홈에 **끌어다** 꽂기 (지금은 눌러서 고르기. 데이터 연산은 같으므로 UI 만 얹으면 됨)
- 홈에 꽂힌 블록을 캔버스로 도로 빼내기 (지금은 시트에서 삭제)
- 캔버스 블록에 고른 빌드 이름이 안 보임 (display 가 저장소를 모름)
- `build` typeId 가 팔레트 블록이자 캔버스 덩어리로 이중 사용됨
- 타임라인 표현력: 터치/대기 외 블록은 타임라인으로 못 여는 문제

## 먼 계획 (기반만 열어둔 상태)

지금 만들지 않되 **막지도 않는다**. 지키는 원칙 셋:
`ExecContext` 는 확장 가능하게 / 실행 시작·정지는 단일 창구로 / 닫힌 나열을 새로 만들지 않기.

- API Material (Gemini 등) — 능력은 `ExecContext` 에 필드로 더함, 키는 별도 보안 저장소에 별칭 참조
- 커스텀 Material (사용자 스크립트) — 외부 Material 반입은 한 관문으로 모아 두기(나중에 권한 동의를 끼울 자리)
- 백그라운드 실행 — "다른 앱 사용 중"까지. 화면 완전 오프는 터치 매크로에 OS 벽
- 사진 인식 · 킬 스위치 · 휴먼라이즈 · 오버레이 버튼=Material 바인딩

## 규칙

- 각 작업이 끝난 시점에 빌드는 항상 초록 (점진적 교체)
- 하드코딩 대신 기반부터. 새 블록 추가는 "BlockDef 한 줄 + Executor 하나"로 끝나야 함
- 미사용 import 는 ktlint(`.editorconfig` 에 no-unused-imports)로 정리
