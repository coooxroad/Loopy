# Loopy 진행 상황

**작업 N = 테마 하나.** 그 안의 커밋은 번호 없이 서술형으로 남긴다
(`슬롯편집: 모자 결합 규칙 일원화`). 작업이 끝나거나 단계가 넘어갈 때 이 파일도 같이 고친다.

저장소 밖 문서는 반드시 뒤처지므로, "지금 어디까지 됐나"의 답은 항상 여기다.

---

## 끝난 작업

| # | 테마 | 남긴 것 |
|---|---|---|
| 5 | 타입 정의 일원화 | MaterialType/MaterialRegistry 를 BlockDef 로 흡수. 도메인은 kind 만 앎 |
| 6 | 데이터 통합 | 타임라인이 캔버스 덩어리 구조를 보존. 죽은 macro//Migration 삭제 |
| 7 | 에디터 상태 홀더 | 순수 EditorUi/EditorEvent/reduce. 화면은 그리기만 |
| 8 | God object 분해 | OverlayService 763→616, MainActivity 698→324 |
| 9 | 코드 위생 · 기하 정리 | BlockSpec 이중화 종료, 미사용 import 113개 + ktlint 설정, C 맞물림 세로(-3nd)·가로(벽=INDENT), 오른쪽아래 모서리, 무한 모눈, 유색 번짐 |
| 10 | 빌드 불러오기 | ExecContext=능력가방(data class), MaterialSource port, BuildPick + 빌드 선택 UI |
| 11 | 슬롯 편집 | Material.slots·직렬화·Evaluator 뼈대, 비교 블록, 홈에 꽂기, 트리연산 슬롯 인지, 결합 규칙을 `canPlaceAt` 한 곳으로(모자 포함) |

## 지금 하는 일 — 작업 12: 빌더 조립 UI

블록을 **모양 그대로 보고, 끌어다 조립**하게 만든다. Material 다량 추가의 선행 조건.

- [x] **A. 공용 블록 렌더러** — `BlockFace` 추출. 팔레트가 캔버스와 같은 그림(육각·둥근·C·모자)으로 표시. 설명 글줄 제거
- [x] **B. 하드코딩 걷어내기** — `typeId == "..."` 분기 제거. 전용 편집기(`opensEditor`)·갈래(`parallel`)·시간축(`TimelineRole`) 선언화, 덩어리 개념에 이름(`Clump`)
- [ ] **C. 팔레트를 트레이로** — 접히는 하단 트레이, 카테고리 색 레일, 끌어서 꺼내기
- [ ] **D. 소켓 드롭 + 팝오버** — 조건도 블록 조립으로. 홈 고르기 시트 제거

### B 결과

| 위치 | 전 | 후 |
|---|---|---|
| `BlockCanvas` | `typeId == "touch"` → 전용 편집기 | `def.opensEditor` (축만 알려주고 화면은 밖에서 결정) |
| `BlockCanvas` | `typeId == "parallel"` → 갈래 버튼 | `def.parallel` (선언은 이미 있었는데 아무도 안 읽었음) |
| `Timeline` | `wait`/`touch`/`parallel`/`build` 나열 | `TimelineRole` 5종. 기본이 NONE 이라 새 블록은 안전하게 "표현 못 함" |
| 여러 곳 | `typeId == "build"` (덩어리) | `Clump.isClump` / `Clump.TYPE_ID` 한 곳 |

이제 **새 블록을 더해도 캔버스·타임라인을 고칠 일이 없다.** 시간축에 나타내고 싶으면
정의에 `timeline = ...` 한 줄만 붙이면 된다.

남겨 둔 것: `Builtins` 의 멀티터치 최적화(`simpleStroke`)는 실행 성능 관련이라 이번엔 두었다.
새 블록이 안 맞으면 일반 병렬 실행으로 안전하게 빠지므로 급하지 않다.

## 다음에 볼 것

- 홈에 꽂힌 블록을 캔버스로 도로 빼내기 (지금은 시트에서 삭제)
- 캔버스 블록에 고른 빌드 이름이 안 보임 (display 가 저장소를 모름)
- `build` typeId 가 팔레트 블록이자 캔버스 덩어리로 이중 사용됨
- 팔레트 검색 (블록 50개 넘어가면 필요)

## 그다음 — Material 다량 추가

지금 14개는 초라하다. 작업 12 가 끝나면 블록을 대량으로 늘린다.
전제: 정의 한 줄 + 실행기 하나로 끝나야 하고, 팔레트·타임라인이 저절로 따라와야 한다.

## 먼 계획 (기반만 열어둔 상태)

지금 만들지 않되 **막지도 않는다**. 지키는 원칙 셋:
`ExecContext` 는 확장 가능하게 / 실행 시작·정지는 단일 창구로 / 닫힌 나열을 새로 만들지 않기.

- API Material (Gemini 등) — 능력은 `ExecContext` 에 필드로, 키는 보안 저장소에 별칭 참조
- 커스텀 Material (사용자 스크립트) — 외부 반입은 한 관문으로 모아 두기(권한 동의를 끼울 자리)
- 백그라운드 실행 — "다른 앱 사용 중"까지. 화면 완전 오프는 터치 매크로에 OS 벽
- 사진 인식 · 킬 스위치 · 휴먼라이즈 · 오버레이 버튼=Material 바인딩

## 규칙

- 각 단계가 끝난 시점에 빌드는 항상 초록 (점진적 교체)
- 하드코딩 대신 기반부터. 새 블록 추가는 "BlockDef 한 줄 + Executor 하나"로 끝나야 함
- 같은 것을 두 곳에서 그리거나 계산하지 않기 (팔레트=캔버스, 레이아웃=그리기)
- 미사용 import 는 ktlint(`.editorconfig` 의 no-unused-imports)로 정리
